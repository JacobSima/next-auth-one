/**
 * These are providers api end points, they are always public
 * example: http://localhost:3000/api/auth/providers (http://localhost:3000/api/auth/signin/github)
 * currently we set the github provider
 */
export {GET, POST} from "@/auth"